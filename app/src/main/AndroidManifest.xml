<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.krava.vkmedia">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".application.VKApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".application.ui.activity.MainActivity"/>
        <activity android:name=".application.ui.activity.DownloadsActivity"/>
        <activity android:name=".application.ui.activity.LoginActivity">
            <intent-filter>
                <data android:scheme="vk4935807" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".application.ui.activity.AuthTransActivity"
            android:theme="@style/Theme.MyTransparent"/>
        <activity android:name=".application.ui.activity.ActivityAudioPlayer"
            android:theme="@style/PlayerTheme" android:launchMode="singleTask"/>
        <activity android:name="com.vk.sdk.VKServiceActivity" android:label="ServiceActivity" android:theme="@style/VK.Transparent" />

        <service android:name=".application.ui.service.AudioPlayerService"/>
    </application>

</manifest>